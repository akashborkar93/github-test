name: my_workflows

on: push

jobs: 
    first_job:
        runs-on: ubuntu-latest
        steps: 
        - name: Checkout-repo
          uses: actions/checkout@v4

        - name: list
          run: |
            echo "My first git hub action job"
            ls -ltra
            cat READMR.md
